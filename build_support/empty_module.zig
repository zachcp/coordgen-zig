//! Build-graph placeholder for approved layers whose implementation bead has
//! not yet created a source root. Each layer remains a distinct Module with
//! its own reviewed import table even though this root currently has no code.
