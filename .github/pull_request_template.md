## Tracker

Bead: cgz-...

<!--
Name every Bead this change implements. `Bead:` and `Beads:` are both accepted,
and the same trailer may instead appear in a commit message. Each referenced
Bead must be claimed and published with `bd dolt push` before the PR opens.
Closing it during the PR is valid; reusing a Bead that was already closed when
the PR opened is not.
-->
